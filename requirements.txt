# 보호적 버전 고정: 하한(검증된 최소) + 메이저 상한(예기치 않은 파괴적 변경 차단)
# 특히 yfinance/streamlit은 업데이트가 잦아 상한이 없으면 어느 날 갑자기 깨질 수 있음.
# 코드는 pandas 2.x / numpy 1.x~2.x 기준으로 작성됨 → 메이저 상한으로 고정.

streamlit>=1.35.0,<2.0.0
yfinance>=0.2.40,<0.3.0
pandas>=2.0.0,<3.0.0
numpy>=1.26.0,<3.0.0
pyarrow>=15.0.0
requests>=2.31.0,<3.0.0
lxml>=5.0.0
html5lib>=1.1
beautifulsoup4>=4.12.0,<5.0.0
matplotlib>=3.8.0,<4.0.0
plotly>=5.20.0,<7.0.0
scipy>=1.11.0,<2.0.0

# 개발/테스트용 (실행에는 불필요)
# pytest>=8.0.0
